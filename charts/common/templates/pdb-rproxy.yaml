{{- $pdbEnabled := (.Values.canary.reverseProxy.podDisruptionBudget | default dict).enabled -}}
{{- if eq $pdbEnabled nil -}}{{- $pdbEnabled = .Values.podDisruptionBudget.enabled -}}{{- end -}}
{{- if and $pdbEnabled .Values.canary.enabled }}
{{- include "common.pdb" (dict "root" . "name" (include "common.reverseProxyName" .) "selectorTemplate" "common.reverseProxySelectorLabels" "override" .Values.canary.reverseProxy.podDisruptionBudget) }}
{{- end }}
