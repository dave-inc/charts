{{- $pdbEnabled := (.Values.canary.podDisruptionBudget | default dict).enabled -}}
{{- if eq $pdbEnabled nil -}}{{- $pdbEnabled = .Values.podDisruptionBudget.enabled -}}{{- end -}}
{{- if and $pdbEnabled .Values.canary.enabled }}
{{- include "common.pdb" (dict "root" . "name" (include "common.canaryName" .) "selectorTemplate" "common.canarySelectorLabels" "override" .Values.canary.podDisruptionBudget) }}
{{- end }}
