apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ include "cloudarmor.name" . }}
  {{- with .Values.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- toYaml .Values.backendConfig | nindent 2 }}
