name: Helm Lint

on:
  pull_request:
    branches:
      - '**'

env:
  HELM_VERSION: v4.2.4

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # 4.2.2

      - name: Set up Helm
        uses: azure/setup-helm@fe7b79cd5ee1e45176fcad797de68ecaf3ca4814 # v4.2.0
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Lint every chart
        run: |
          set -euo pipefail
          for dir in charts/*/; do
            [ -f "$dir/Chart.yaml" ] || continue
            echo "::group::helm lint $dir"
            # Subcharts must be vendored first. helm lint only warns about missing
            # dependencies, but helm package later fails outright.
            helm dependency update "$dir"
            helm lint "$dir"
            echo "::endgroup::"
          done

      # release-please only releases charts it is configured for. A chart missing
      # from either file is silently frozen at its current version, which is the
      # kind of failure nobody notices until a release is expected and absent.
      - name: Verify release-please covers every chart
        run: |
          set -euo pipefail
          fail=0

          charts=$(find charts -mindepth 2 -maxdepth 2 -name Chart.yaml | xargs -n1 dirname | sort)
          configured=$(jq -r '.packages | keys[]' release-please-config.json | sort)
          manifested=$(jq -r 'keys[]' .release-please-manifest.json | sort)

          if ! diff <(echo "$charts") <(echo "$configured") > /dev/null; then
            echo "::error::release-please-config.json packages do not match chart directories"
            diff <(echo "$charts") <(echo "$configured") || true
            fail=1
          fi

          if ! diff <(echo "$charts") <(echo "$manifested") > /dev/null; then
            echo "::error::.release-please-manifest.json entries do not match chart directories"
            diff <(echo "$charts") <(echo "$manifested") || true
            fail=1
          fi

          # The helm strategy derives the release tag component from Chart.yaml's
          # `name`, while chart-releaser derives it from the directory name. If they
          # disagree the two tools tag different things.
          for dir in $charts; do
            name=$(yq -r '.name' "$dir/Chart.yaml")
            base=$(basename "$dir")
            if [ "$name" != "$base" ]; then
              echo "::error::$dir/Chart.yaml name '$name' does not match directory '$base'"
              fail=1
            fi
          done

          exit $fail

      # SOC-CI runs validate_pull_request against release-please's own release PR
      # and fails it unless the title or body resolves to an open Jira ticket. The
      # only bypass in soc-ci-gha-workflow.yml is a hardcoded Dependabot user id,
      # which the release bot does not have. The footer carries that reference.
      - name: Verify release PR will satisfy SOC-CI
        run: |
          set -euo pipefail
          footer=$(jq -r '.["pull-request-footer"] // ""' release-please-config.json)

          if [ -z "$footer" ]; then
            echo "::error::pull-request-footer is missing. The release PR will fail SOC-CI."
            exit 1
          fi

          # Same expression validate_jira.py uses.
          if ! echo "$footer" | grep -qE '\b[A-Z0-9]{1,10}-[0-9]+\b'; then
            echo "::error::pull-request-footer has no Jira ticket reference."
            exit 1
          fi

          if echo "$footer" | grep -qE '\bSRE-0000\b'; then
            echo "::error::pull-request-footer still has the SRE-0000 placeholder."
            echo "::error::Replace it with a real, permanently open ticket."
            exit 1
          fi

      # Backstop only. The Bundle Schemas workflow regenerates and commits this
      # file on the PR, so this should normally pass without doing anything. It
      # still matters for fork PRs, which that workflow cannot push to, and for
      # any PR that does not touch schemas/ and so never triggers it.
      - name: Verify values.schema.json is current
        run: |
          set -euo pipefail
          make schemas
          if ! git diff --exit-code -- '**/values.schema.json'; then
            echo "::error::values.schema.json is stale. Run 'make schemas' and commit the result."
            exit 1
          fi
